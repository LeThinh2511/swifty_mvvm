//
//  {{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

class {{ module_info.name }}ViewModel {
    private weak var view: I{{ module_info.name }}View?
    private var navigator: I{{ module_info.name }}Navigator?

    init(view: I{{ module_info.name }}View, navigator: I{{ module_info.name }}Navigator) {
        self.view = view
        self.navigator = navigator
    }
}

// MARK: I{{ module_info.name }}ViewModel
extension {{ module_info.name }}ViewModel: I{{ module_info.name }}ViewModel {
    func viewIsReady() {
        view?.setupInitialState()
    }
}
