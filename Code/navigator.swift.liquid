//
//  {{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit

extension {{ module_info.name }}Navigator {
    enum Transition {
    }
}

class {{ module_info.name }}Navigator {
    var viewController: {{ module_info.name }}ViewController
    
    init() {
        viewController = {{ module_info.name }}ViewController()
        let viewModel = {{ module_info.name }}ViewModel(view: viewController)
        viewController.viewModel.navigator = self
    }
    
    func performTransition(transition: Transition) {
    }
}
